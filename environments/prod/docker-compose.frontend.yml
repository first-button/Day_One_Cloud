services:
  frontend:
    image: ${ECR_REGISTRY}/day-one-prod/frontend:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped
